package Pole.Pole;

public class PolPole {
	public PolPole() {
StoPrvkovePole pole = new StoPrvkovePole();
		int data = pole;
		int[] lava = new int[(data + 1) / 2];
		int[] prava = new int[data - lava.length];
		for (int i = 0; i < data1; i++) {
			if (i < lava.length) {
				lava[i] = pole[i];
			} else {
				prava[i - lava.length] = pole[i];
			}

		}
	}
}
